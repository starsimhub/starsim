[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ssr-engine"
version = "0.1.0"
description = "Fast modular Rust engine for Starsim"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "ssr_engine"
