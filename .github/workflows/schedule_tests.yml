name: Scheduled Starsim tests
on:
  workflow_call:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *" # Run every day at midnight UTC

jobs:
  ci-tests:
    uses: ./.github/workflows/tests.yml
  downstream-tests:
    uses: ./.github/workflows/test_downstream.yml
